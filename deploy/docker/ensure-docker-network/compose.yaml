networks:
  net:
    external: false
    name: ${NETWORK_NAME:-traefik-public}
    driver: ${NETWORK_DRIVER:-overlay}
    attachable: ${NETWORK_ATTACHABLE:-true}

services:
  pause:
    image: ${IMAGE_NAME:-k8s.gcr.io/pause}:${IMAGE_VERSION:-3.9}
    networks:
      - net
